interface:
  display_name: "Brevia Card"
  short_description: "Repo-specific patterns and workflows for Brevia-Card"
  default_prompt: "Use the Brevia-Card repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true